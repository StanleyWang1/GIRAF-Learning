# Cable-stringing collection output; runtime settings come from the shared base.
extends: data_collection.yaml

dataset:
  output_dir: data/cable_stringing
  zarr_name: boom_40in.zarr
  episode_dir_name: boom_40in_videos
