# Ring-over-peg collection output; runtime settings come from the shared base.
extends: data_collection.yaml

dataset:
  output_dir: data/ring_over_peg_redo
  zarr_name: boom_40in.zarr
  episode_dir_name: boom_40in_videos
