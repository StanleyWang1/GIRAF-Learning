# Tape-grasping collection output; runtime settings come from the shared base.
extends: data_collection.yaml

dataset:
  output_dir: data/tape_grasping
  zarr_name: sept03_trials.zarr
  episode_dir_name: sept03_trials_videos
